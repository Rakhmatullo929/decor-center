# Django backend origin (no trailing slash)
REACT_APP_HOST_API=http://localhost:8000

# Optional: force API language when no i18n value stored (uz | ru)
# REACT_APP_DEFAULT_API_LANG=uz

# Optional: frontend-only auth mock (no backend needed)
# REACT_APP_AUTH_MOCK=true
