services:
  db:
    image: postgres:18
    environment:
      POSTGRES_DB: decor
      POSTGRES_USER: decor
      POSTGRES_PASSWORD: decor
    ports:
      # 5433 on the host by default (local Postgres already occupies 5432); override with
      # DB_HOST_PORT if 5433 is taken too, e.g. DB_HOST_PORT=5434 docker compose up -d db.
      - "${DB_HOST_PORT:-5433}:5432"
    volumes:
      # postgres:18 keeps PGDATA under /var/lib/postgresql/18/, not .../data
      - pgdata:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U decor -d decor"]
      interval: 5s
      timeout: 3s
      retries: 12

  backend:
    build: ./backend
    command: >
      sh -c "python manage.py migrate &&
             python manage.py seed_initial_data &&
             python manage.py seed_surveys &&
             python manage.py seed_survey_content &&
             python manage.py seed_demo_data &&
             python manage.py runserver 0.0.0.0:8000"
    env_file:
      - backend/.env
    environment:
      DATABASE_URL: postgres://decor:decor@db:5432/decor
    ports:
      # Override BACKEND_HOST_PORT if 8000 is taken, e.g. BACKEND_HOST_PORT=8010.
      - "${BACKEND_HOST_PORT:-8000}:8000"
    volumes:
      # Bind-mount the source so edits on the host are picked up by Django's autoreloader
      # without rebuilding the image. Safe to shadow /app entirely — pip installs packages
      # into the image's system site-packages, not under /app.
      - ./backend:/app
      # Persist uploaded media (employee photos / face samples) across container
      # rebuilds and restarts — otherwise enrolled faces are wiped on every `up`.
      - media:/app/media
    depends_on:
      db:
        condition: service_healthy

  frontend:
    build: ./frontend
    environment:
      # The browser runs on the host, so it reaches the backend via localhost.
      # Point this at the backend's published host port (BACKEND_HOST_PORT).
      REACT_APP_HOST_API: ${REACT_APP_HOST_API:-http://localhost:8000}
      # Docker Desktop's bind-mount file events (macOS/Windows) don't always reach
      # inotify reliably — force CRA's watcher to poll so edits always trigger a reload.
      CHOKIDAR_USEPOLLING: "true"
      WATCHPACK_POLLING: "true"
    ports:
      # Override FRONTEND_HOST_PORT if 3000 is taken, e.g. FRONTEND_HOST_PORT=3010.
      - "${FRONTEND_HOST_PORT:-3000}:3000"
    volumes:
      # Bind-mount the source so edits on the host are picked up by the CRA dev server
      # without rebuilding the image. Anonymous volume for node_modules keeps the image's
      # installed (Linux-built) packages from being shadowed by the host tree.
      - ./frontend:/app
      - /app/node_modules
    # CRA dev server exits without an attached stdin/tty
    stdin_open: true
    tty: true
    depends_on:
      - backend

volumes:
  pgdata:
  media:
