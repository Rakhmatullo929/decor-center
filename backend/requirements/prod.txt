-r base.txt
gunicorn~=23.0
whitenoise~=6.7
# The face-recognition stack (insightface / onnxruntime / opencv-python-headless) now lives
# in base.txt so dev and prod share it. Enable the real backend via DECOR_FACE_BACKEND.
