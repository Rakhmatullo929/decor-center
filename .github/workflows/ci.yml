name: CI

# Integration checks for main + all PRs. Does NOT deploy — prod ships only from the
# `production` branch (see deploy-production.yml).
on:
  pull_request:
  push:
    branches: [main]

jobs:
  tests:
    uses: ./.github/workflows/tests.yml
